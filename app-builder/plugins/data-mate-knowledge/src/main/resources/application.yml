fit:
  beans:
    packages:
      - 'modelengine.fit.jade.datamate.knowledge'
# 默认使用 ME 集群内 DataMate 服务名，可通过环境变量或 profile 覆盖
# timeout: 访问超时时间（秒），默认 30，可按场景自定义
datamate:
  url:
    list: 'http://datamate-gateway:8080/api/knowledge-base/list'
    retrieve: 'http://datamate-gateway:8080/api/knowledge-base/retrieve'
  # timeout: 30

